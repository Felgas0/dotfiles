# General settings outside the block
ipc = off
splash = false

# New block-based wallpaper configuration
wallpaper {
    # Leave monitor empty for all monitors (fallback)
    monitor = 
    path = /home/felgas/.config/backgrounds/background.jpg
    # Optional: cover, contain, stretch, or fill
    fit_mode = cover
}
